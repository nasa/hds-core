dist/
node_modules/
*.min.css
*.min.css.map
*.min.js

# Generated by Style Dictionary
src/scss/_hds-tokens.scss
src/scss/base/_custom-properties.scss

# NOTE: patterns in this file resolve relative to .config/, not the repo
# root, so entries above need a ../ prefix to match anything. Left as-is
# because .gitignore (also passed via --ignore-path) already covers dist/
# and node_modules/, and .scss falls outside the format glob.
