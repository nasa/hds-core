---
'@nasa-hds/core': patch
---

Restore sortable-table icons and keyboard focus outlines in forced-colors mode using the existing USWDS SVG fallback. Normal palette rendering and sorting behavior are unchanged.
